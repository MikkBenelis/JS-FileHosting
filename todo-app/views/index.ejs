<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8">
		<title>BSUIR JS Labs</title>
		<link rel="stylesheet" href="styles.css">
		<link rel="icon" href="favicon.png">
	</head>
	<body>
		<div class="container">
			<div class="title">TODO List</div>
			<form method="post" enctype="multipart/form-data" class="form">
				<div class="newTaskArea">
					<input type="text" name="newTaskText" placeholder="New task" class="newTaskText"/>
					<label for="newTaskFileId" class="newTaskFile">Attach</label>
					<input type="file" name="newTaskFile" id="newTaskFileId" style="display: none"/>
					<input formaction="/addtask" type="submit" value="Add" class="newTaskAddButton"/>
				</div>
				<ul class="taskList">
					<% for(var i = 0; i < taskList.length; i++){ %>
						<li class="taskListItem">
							<input type="checkbox" name="check" value="<%= taskList[i].id %>" class="taskCheckbox"/>
							<p>
								<%= taskList[i].text + " #" + taskList[i].id %>
								<% if (taskList[i].attachment != null) { %>
									(<a href="<%= taskList[i].attachment %>" download="attachment">file</a>)
								<% } %>
							</p>
						</li>
					<% } %>
				</ul>
				<input formaction="/checktask" type="submit" value="Mark as completed" class="checkTaskButton"/>
				<input formaction="/removetask" type="submit" value="Remove" class="removeTaskButton"/>
			</form>
			<div class="completedTasksTitle">Completed tasks</div>
			<ul class="completedTasksList">
				<% for(var i = 0; i < completedTasks.length; i++){ %>
					<li class="taskListItem">
						<p>
							<%= completedTasks[i].text + " #" + completedTasks[i].id %>
							<% if (completedTasks[i].attachment != null) { %>
								(<a href="<%= completedTasks[i].attachment %>" download="attachment">file</a>)
							<% } %>
						</p>
					</li>
				<% } %>
			</ul>
		</div>
	</body>
</html>